# TODO — superseded, see STATE

**The live list is `docs/STATE.md` § PENDING.** The June "Slice 1–4 / V2" checklist that
lived here shipped in different shapes than planned and is fully superseded:

- Slice 1 (foundation/warm) → shipped 2026-06 (server policy, warm toggle, leases later).
- Slice 2 (snappy companion) → became `q` with intents, `--format`, envelopes (docs/q-spec.md).
- Slice 3 (vision parse) → became `see` with --ui/--ocr/--crop + the artifact store.
- Slice 4 (image-gen) → became `imagine` on mflux (ComfyUI path dropped).
- V2 items (LAN offload, qwen3-vl) → not pursued; revisit only via STATE § PENDING.

Nothing here is actionable — kept as a historical marker so old references resolve.
